  <div class="send-a-message">
    <h1>Connect with <%= @startup.industry %></h1>
    <%= simple_form_for [startup, connection] do |f| %>
      <%= f.input :message, as: :text, html5: true, placeholder: "Type your message here, if you want" %>
      <%= f.submit %>
    <% end %>
  </div>
