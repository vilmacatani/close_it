<!DOCTYPE html>
<html>
  <head>
    <title>CloseIt</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script src="https://unpkg.com/feather-icons"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
  <%# <% home_user = current_page?(root_path) && !current_user %>
  <%# <% blue_pages = home_user || current_page?(new_user_session_path) || current_page?(controller: 'startups', action: 'show') || current_page?(controller: 'startups', action: 'team') || current_page?(user_investor_path) %> %> %>
  <%# add startup path show and OTHERS!!! %>
  <div class="grey-back <%= 'blue-back' if blue_pages %> full-screen">
    <div class= "all-container">
      <%= render "shared/navbar" %>
      <%= render "shared/flashes" %>
      <%= yield %>
      <%= render "shared/footer" %>
    </div>
  </div>
  <script>
    feather.replace()
  </script>
  </body>

</html>
